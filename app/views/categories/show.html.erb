<h1 align="center"><%= "Category: " + @category.name %></h1>

<p><%= link_to "Create new article...", new_article_path %></p>

<div align="center">
    <% if logged_in? and current_user.admin? %>
        <div class="row"><span class="badge">
            <%= link_to 'Edit Category', edit_category_path(@category) %></span>
        </div>
    <% end %>
    <%= will_paginate @category_articles %>
</div>

<%= render 'articles/article', objs: @category_articles %>

<div align="center">
    <%= will_paginate @category_articles %>
</div>